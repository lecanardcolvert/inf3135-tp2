#ifndef UTILS_H
#define UTILS_H

/**
 * Duplicate a string
 *
 * Note: the resulting string should be freed when not needed anymore.
 *
 * @param s  The string to duplicate
 * @return   A new string with the same content
 */
char *utils_strdup(const char *s);

#endif
